---
/**
 * Development shell only (Phase 2A). Lifecycle "draft", page type "utility":
 * always noindex, never in the sitemap, and a production build fails while it exists.
 * Contains no sewer/SEO content, local claims, contractor information, or forms.
 */
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout pageId="dev-shell">
  <h1>Development build</h1>
  <p>
    This is a non-production build of the Indy Sewer Resource site foundation. It is not a
    published page and contains no published content.
  </p>
  <p>Status: <strong>not indexable</strong> (lifecycle: draft).</p>
</BaseLayout>
